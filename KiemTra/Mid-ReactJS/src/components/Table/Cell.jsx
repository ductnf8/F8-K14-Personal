import React from 'react';
import {TableCell} from "@mui/material";

const Cell = ({value}) => {
    return (
        <TableCell>
            {value}
        </TableCell>
    );
};

export default Cell;
